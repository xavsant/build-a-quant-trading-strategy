# Building a Quant Trading Strategy

### Dependencies
1. Create a virtualenv with python=3.12
2. pip install torch polars pytorch requests scipy matplotlib altair tqdm vegafusion

_Note: you will need to install some other packages along the way with respect to notebook kernels and vega_
